
## Frontend Setup

1. Copy `environments/environment.example.ts` to `environments/environment.ts` and configure as needed
2. Install Angular CLI globally if not already installed:
   ```bash
   npm install -g @angular/cli
   ```
3. Start the development server:
   ```bash
   ng serve
   ```
